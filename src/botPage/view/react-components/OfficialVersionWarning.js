import React from 'react';
import PropTypes from 'prop-types';

const OfficialVersionWarning = ({ show }) => null;

OfficialVersionWarning.propTypes = {
    show: PropTypes.bool.isRequired,
};

export default OfficialVersionWarning;